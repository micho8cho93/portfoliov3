---
courseId: intro-to-linux
moduleId: "01"
moduleTitle: Terminal Foundations
title: Terminal Flag Hunt
description: Navigate a Linux-style file system and uncover a hidden flag.
order: 1
estimatedMinutes: 25
objectives:
  - Use `ls` to inspect directory contents.
  - Reveal hidden entries with `ls -a`.
  - Move between directories using `cd` and `cd ..`.
  - Read file contents with `cat` to discover a flag.
updatedDate: 2026-02-11
---

import LinuxTerminal from '../../../../components/course/LinuxTerminal.astro';

# Mission

Use the terminal below to find the hidden flag.

## Rules

1. Use only terminal commands in the emulator.
2. Do not refresh to skip steps.
3. Aim to use every required command at least once.

## Terminal Lab

<LinuxTerminal />

## Debrief

When you find the flag, explain which command sequence got you there and why `ls -a` mattered.
